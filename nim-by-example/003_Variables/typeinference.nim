# variable types are inferred in the const, let and var declaratons.
var y = "foo"

echo "the value of y is : ", y

var x = 5
# Assigning a value of a different type will result in a compile time error.
# x = y
